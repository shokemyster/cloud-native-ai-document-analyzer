# Static assets

Vite will copy files from this directory without processing them. Imported images and styles should normally live under `src/` so the build system can fingerprint and optimize them.

