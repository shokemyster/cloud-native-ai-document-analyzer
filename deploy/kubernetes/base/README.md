# Kubernetes base

This directory will contain reusable Deployments, Services, StatefulSets, configuration references, and other environment-neutral resources. Secret values will never be committed here.

