# Kubernetes

This directory will contain native Kubernetes manifests organized with Kustomize.

- `base/` defines environment-neutral resources.
- `overlays/` applies environment-specific configuration without duplicating the base.

