# Docker

This directory will contain container-specific support files. Dockerfiles may remain near their build contexts when that produces clearer and safer builds; shared container scripts or configuration belong here.

