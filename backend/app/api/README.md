# API layer

This directory will contain FastAPI routers, request and response schemas, and HTTP-specific dependency wiring. Routes should validate and translate data, then delegate work to application services.

