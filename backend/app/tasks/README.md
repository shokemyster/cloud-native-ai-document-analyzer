# Background task adapters

This directory will contain thin Celery task entry points. Tasks will deserialize identifiers and invoke application use cases; analysis logic itself will not live in Celery functions.

