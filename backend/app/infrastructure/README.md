# Infrastructure layer

This directory will contain concrete adapters for PostgreSQL, Redis, object storage, file parsing, and AI providers. Keeping adapters here makes external technology replaceable and keeps the core testable.

