# Database migrations

This directory will hold versioned PostgreSQL schema changes, managed later with Alembic. Schema evolution is kept separate from runtime application code and must be reproducible in every environment.

